import {useEffect,useState} from 'react';import {api,patch,post} from '../lib/api';import {Member} from '../types/api';
export function Members(){const [members,setMembers]=useState<Member[]>([]),[form,setForm]=useState({display_name:'',email:'',username:'',password:''});const load=()=>api<Member[]>('/members').then(setMembers);useEffect(()=>{void load()},[]);return <div className="space-y-6"><h1 className="text-3xl font-semibold">Department Members</h1><form onSubmit={async e=>{e.preventDefault();await post('/members',form);setForm({display_name:'',email:'',username:'',password:''});void load()}} className="card grid gap-3 p-4 md:grid-cols-5"><input placeholder="Display name" value={form.display_name} onChange={e=>setForm({...form,display_name:e.target.value})} className="rounded bg-bg p-3"/><input placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="rounded bg-bg p-3"/><input placeholder="Username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})} className="rounded bg-bg p-3"/><input placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="rounded bg-bg p-3"/><button className="rounded bg-indigo-500 px-4 font-semibold">Add</button></form><div className="card overflow-hidden"><table className="w-full text-sm"><thead className="bg-black/20 text-left text-slate-400"><tr><th className="p-3">Name</th><th>Email</th><th>Status</th><th></th></tr></thead><tbody>{members.map(m=><tr className="border-t border-border" key={m.id}><td className="p-3">{m.display_name}</td><td>{m.email}</td><td>{m.active?'Active':'Inactive'}</td><td><button onClick={async()=>{await patch(`/members/${m.id}`,{active:!m.active});void load()}} className="rounded border border-border px-3 py-1">Toggle</button></td></tr>)}</tbody></table></div></div>}
