import {useEffect,useState} from 'react';import {Bar,BarChart,CartesianGrid,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {api} from '../lib/api';
export function Analytics(){const [overview,setOverview]=useState<any>(),[quarters,setQuarters]=useState<any[]>([]),[heat,setHeat]=useState<any>();useEffect(()=>{api('/analytics/overview').then(setOverview);api<any[]>('/analytics/quarters').then(setQuarters);api('/analytics/heatmap').then(setHeat)},[]);const qdata=quarters.map(q=>({quarter:q.quarter,participation:Object.keys(q.sent||{}).length}));return <div className="space-y-6"><h1 className="text-3xl font-semibold">Analytics</h1><div className="grid gap-4 md:grid-cols-3"><div className="card p-4">Members: {overview?.total_members||0}</div><div className="card p-4">Completion: {Math.round(overview?.completion_rate||0)}%</div><div className="card p-4">Planned: {overview?.total_planned||0}</div></div><div className="grid gap-6 xl:grid-cols-2"><div className="card p-4"><h2>Participation</h2><ResponsiveContainer width="100%" height={280}><LineChart data={qdata}><CartesianGrid stroke="#2a2d3e"/><XAxis dataKey="quarter"/><YAxis/><Tooltip/><Line dataKey="participation" stroke="#6366f1"/></LineChart></ResponsiveContainer></div><div className="card p-4"><h2>Quarter rows</h2><ResponsiveContainer width="100%" height={280}><BarChart data={qdata}><CartesianGrid stroke="#2a2d3e"/><XAxis dataKey="quarter"/><YAxis/><Tooltip/><Bar dataKey="participation" fill="#22c55e"/></BarChart></ResponsiveContainer></div></div><div className="card overflow-auto p-4"><h2>Heatmap totals</h2><pre className="text-xs text-slate-400">{JSON.stringify(heat,null,2)}</pre></div></div>}
