import {useState} from 'react';import {post} from '../lib/api';import {User} from '../types/api';
export function Login({setUser}:{setUser:(u:User)=>void}){const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setErr('');try{const r=await post<{user:User}>('/auth/login',{username,password});setUser(r.user)}catch(e:any){setErr(e.message)}}return <div className="grid min-h-screen place-items-center bg-bg p-4"><form onSubmit={submit} className="card glow w-full max-w-md p-8"><h1 className="text-2xl font-bold">Quarterly Points</h1><p className="mt-2 text-slate-400">Sign in to see your giving plan.</p><label className="mt-6 block text-sm">Username<input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" className="mt-2 w-full rounded-lg border border-border bg-bg p-3"/></label><label className="mt-4 block text-sm">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" className="mt-2 w-full rounded-lg border border-border bg-bg p-3"/></label>{err&&<p className="mt-4 text-sm text-red-400">{err}</p>}<button className="mt-6 w-full rounded-lg bg-indigo-500 p-3 font-semibold">Login</button><p className="mt-4 text-xs text-slate-500">Use the admin account created during first-run setup.</p></form></div>}
