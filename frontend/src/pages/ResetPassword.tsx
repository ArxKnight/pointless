import {useState} from 'react';import {Link,useNavigate,useParams} from 'react-router-dom';import {post} from '../lib/api';
export function ResetPassword(){const {token=''}=useParams();const nav=useNavigate();const[pw,setPw]=useState(''),[confirm,setConfirm]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState(''),[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setErr('');setMsg('');if(pw!==confirm){setErr('New passwords do not match');return}try{await post('/auth/password-reset/confirm',{token,new_password:pw});setDone(true);setMsg('Your password has been reset. You can now log in.');setTimeout(()=>nav('/login'),1800)}catch(e:any){setErr(e.message)}}return <div className="grid min-h-screen place-items-center bg-bg p-4"><form onSubmit={submit} className="card glow w-full max-w-md p-8"><h1 className="text-2xl font-bold">Set a new password</h1><p className="mt-2 text-sm text-slate-400">Choose a new admin password for Pointless.</p><label className="mt-6 block text-sm">New password<input required minLength={8} type="password" value={pw} onChange={e=>setPw(e.target.value)} autoComplete="new-password" className="mt-2 w-full rounded-lg border border-border bg-bg p-3"/></label><label className="mt-4 block text-sm">Confirm new password<input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password" className="mt-2 w-full rounded-lg border border-border bg-bg p-3"/></label>{err&&<p className="mt-4 text-sm text-red-400">{err}</p>}{msg&&<p className="mt-4 rounded bg-green-500/10 p-3 text-sm text-green-200">{msg}</p>}<button disabled={done} className="mt-6 w-full rounded-lg bg-indigo-500 p-3 font-semibold disabled:opacity-60">Reset password</button><Link to="/login" className="mt-3 block text-center text-sm text-slate-300 underline">Back to login</Link></form></div>}
