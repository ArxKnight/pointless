import {useEffect,useState} from 'react';import {api} from '../lib/api';
export function History(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>('/plans/me/history').then(setRows)},[]);return <div className="space-y-6"><h1 className="text-3xl font-semibold">History</h1>{rows.map(r=><details key={r.quarter.id} className="card p-4"><summary className="cursor-pointer font-semibold">{r.quarter.label}</summary><div className="mt-3 space-y-2">{r.plans.map((p:any)=><div key={p.id} className="rounded bg-black/20 p-3">{p.from_name} → {p.to_name}: <span className="font-mono text-indigo-300">{p.amount}</span> {p.acknowledged?'✓':'pending'}</div>)}</div></details>)}</div>}
