import {useEffect,useState} from 'react';import {api} from '../lib/api';import {MyPlan} from '../types/api';import {QuarterBadge} from '../components/Badges';import {StatCard} from '../components/StatCard';import {PlanCard} from '../components/PlanCard';import {GivingTree} from '../components/GivingTree';
export function Dashboard(){const [data,setData]=useState<MyPlan|null>(null);const load=()=>api<MyPlan>('/plans/me').then(setData);useEffect(()=>{void load()},[]);if(!data)return <p>Loading...</p>;const sent=data.outgoing.filter(p=>p.acknowledged).reduce((s,p)=>s+p.amount,0);return <div className="space-y-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-3xl font-semibold tracking-tight">Dashboard</h1><p className="text-slate-400">Your personalised points instructions.</p></div><QuarterBadge label={data.quarter?.label}/></div><div className="grid gap-4 md:grid-cols-3"><StatCard title="Total to give" value="50"/><StatCard title="Total sent" value={sent}/><StatCard title="Remaining" value={50-sent}/></div><div className="grid gap-6 xl:grid-cols-2"><PlanCard plans={data.outgoing} onChange={load}/><div className="card p-4"><GivingTree member={data.member} outgoing={data.outgoing} incoming={data.incoming}/></div></div></div>}
