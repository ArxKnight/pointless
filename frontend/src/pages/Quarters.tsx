import {useEffect,useMemo,useState} from 'react';
import {api,del} from '../lib/api';
import {Participant,Plan,Quarter,QuarterDetail} from '../types/api';
import {SuccessBanner} from '../components/SuccessBanner';

type GenerationStep={label:string;progress:number}|null;
const GENERATE_TIMEOUT_MS=60000;
const GENERATE_POLL_MS=1500;
const GENERATE_MAX_POLLS=40;
const wait=(ms:number)=>new Promise(resolve=>setTimeout(resolve,ms));
function isAbortError(e:any){return e?.name==='AbortError'}
function quarterStatus(q:Quarter){if(q.is_completed)return 'Completed';if(q.is_active)return 'Active';if(q.status==='published')return 'Generated';return q.status==='generating'?'Generating':'Created'}

export function Quarters(){
 const [quarters,setQuarters]=useState<Quarter[]>([]);
 const [participants,setParticipants]=useState<Participant[]>([]);
 const [selected,setSelected]=useState<number[]>([]);
 const [detail,setDetail]=useState<QuarterDetail|null>(null);
 const [msg,setMsg]=useState('');
 const [success,setSuccess]=useState('');
 const [wizard,setWizard]=useState(false);
 const [busy,setBusy]=useState(false);
 const [generationStep,setGenerationStep]=useState<GenerationStep>(null);
 const [deleteTarget,setDeleteTarget]=useState<Quarter|null>(null);
 const [deleting,setDeleting]=useState(false);
 const [form,setForm]=useState({year:new Date().getFullYear(),quarter:Math.floor(new Date().getMonth()/3)+1,label:''});
 const load=async()=>{setQuarters(await api<Quarter[]>('/quarters'));setParticipants(await api<Participant[]>('/participants?include_inactive=false'))};
 useEffect(()=>{void load()},[]);
 const totals=useMemo(()=>{const sent=new Map<number,number>(),recv=new Map<number,number>();(detail?.plan||[]).forEach(p=>{if(p.from_participant_id)sent.set(p.from_participant_id,(sent.get(p.from_participant_id)||0)+p.amount);if(p.to_participant_id)recv.set(p.to_participant_id,(recv.get(p.to_participant_id)||0)+p.amount)});return{sent,recv}},[detail]);
 function startCreate(){setMsg('');setDetail(null);setSelected(participants.map(p=>p.id));setForm({year:new Date().getFullYear(),quarter:Math.floor(new Date().getMonth()/3)+1,label:''});setGenerationStep(null);setWizard(true)}
 async function openQuarter(q:Quarter){try{const d=await api<QuarterDetail>(`/quarters/${q.id}`);setDetail(d);setSelected(d.participants.map(p=>p.id));setMsg('')}catch(e:any){setMsg(e.message)}}
 async function confirmDeleteQuarter(){if(!deleteTarget)return;setDeleting(true);setMsg('');try{await del(`/quarters/${deleteTarget.id}`);setSuccess(`${deleteTarget.label} has been deleted successfully.`);if(detail?.quarter.id===deleteTarget.id)setDetail(null);setDeleteTarget(null);await load()}catch(e:any){setMsg(e.message)}finally{setDeleting(false)}}
 async function waitForGeneratedQuarter(qid:number){for(let i=0;i<GENERATE_MAX_POLLS;i++){await wait(GENERATE_POLL_MS);const d=await api<QuarterDetail>(`/quarters/${qid}`);if(d.quarter.status==='published'&&d.plan.length>0)return d;setGenerationStep({label:'Generation finished on the server. Checking the generated quarter...',progress:Math.min(90,70+i*2)})}throw new Error('The quarter may have generated, but the page could not confirm it. Refresh Manage Quarters and open the quarter to check its status.')}
 async function generateWithTimeout(){const controller=new AbortController();const timeout=window.setTimeout(()=>controller.abort(),GENERATE_TIMEOUT_MS);try{return await api<{quarter:Quarter;plans:Plan[]}>('/quarters/generate-activate',{method:'POST',body:JSON.stringify({...form,participant_ids:selected}),signal:controller.signal})}catch(e:any){if(!isAbortError(e))throw e;setGenerationStep({label:'The server is still finishing or the response was delayed. Checking the quarter status...',progress:70});const matching=quarters.find(q=>q.year===form.year&&q.quarter===form.quarter);if(!matching)throw new Error('The generation request is still running. Check the container logs for progress, then refresh Manage Quarters.');const recovered=await waitForGeneratedQuarter(matching.id);return{quarter:recovered.quarter,plans:recovered.plan}}finally{window.clearTimeout(timeout)}}
 async function generateAndActivate(){setBusy(true);setMsg('');let progressTimer:number|undefined;try{if(selected.length<2)throw new Error('Select at least two participants.');setGenerationStep({label:'Saving selection and starting generation...',progress:15});setGenerationStep({label:'Generating and activating the balanced points distribution...',progress:45});progressTimer=window.setInterval(()=>setGenerationStep(prev=>prev&&prev.progress<80?{label:'Generating and activating the balanced points distribution...',progress:Math.min(prev.progress+3,80)}:prev),2500);const result=await generateWithTimeout();if(progressTimer){window.clearInterval(progressTimer);progressTimer=undefined}setGenerationStep({label:'Refreshing the generated quarter...',progress:85});const refreshed=await api<QuarterDetail>(`/quarters/${result.quarter.id}`);setGenerationStep({label:'Done. Closing this window...',progress:100});setDetail(refreshed);setWizard(false);await load();setSuccess(`${result.quarter.label} has been generated and activated successfully.`)}catch(e:any){setMsg(e.message||'Quarter generation failed. Check the container logs for the detailed generation steps.')}finally{if(progressTimer)window.clearInterval(progressTimer);setBusy(false);setGenerationStep(null)}}
 const expected=(detail?.participants.length||0)*50; const totalSent=Array.from(totals.sent.values()).reduce((a,b)=>a+b,0);
 return <div className="space-y-6"><SuccessBanner message={success} onClose={()=>setSuccess('')}/><div className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="text-3xl font-semibold">Manage Quarters</h1><p className="text-slate-400 text-sm mt-1">Generate and activate quarters by selecting included participants. There is no draft mode: generated quarters can be opened or deleted.</p></div><button onClick={startCreate} className="rounded bg-indigo-500 px-4 py-2 font-semibold">Generate Quarter</button></div>{msg&&<div className="card p-4 text-sm">{msg}</div>}
  <div className="card overflow-hidden"><table className="w-full text-sm"><thead><tr className="bg-black/20 text-left text-slate-400"><th className="p-3">Quarter</th><th>Status</th><th>Actions</th></tr></thead><tbody>{quarters.length===0?<tr><td colSpan={3} className="p-4 text-slate-400">No current or future quarters. Passed quarters are shown in History.</td></tr>:quarters.map(q=><tr key={q.id} className="border-t border-border"><td className="p-3">{q.label}</td><td>{quarterStatus(q)}</td><td className="space-x-2"><button onClick={()=>openQuarter(q)} className="rounded border border-border px-3 py-1">Open</button><button onClick={()=>setDeleteTarget(q)} className="rounded border border-red-400 px-3 py-1 text-red-200">Delete</button></td></tr>)}</tbody></table></div>
  {detail&&<div className="grid gap-6 xl:grid-cols-[360px_1fr]"><div className="card p-4"><h2 className="font-semibold">{detail.quarter.label}</h2><p className="text-sm text-slate-400">Generated quarters are read-only. Delete and generate a replacement if the participant selection or allocation needs changing.</p><div className="mt-4 rounded bg-black/20 p-3 text-sm">Participants: {detail.participants.length}<br/>Expected total: {expected}<br/>Total sent: {totalSent}<br/>Plan status: {detail.plan.length===0?'No plan generated':totalSent===expected?'Totals look valid':'Invalid total'}</div></div><Preview detail={detail} sent={totals.sent} recv={totals.recv}/></div>}
  {wizard&&<QuarterWizard form={form} setForm={setForm} participants={participants} selected={selected} setSelected={setSelected} onClose={()=>!busy&&setWizard(false)} onGenerateActivate={generateAndActivate} busy={busy} generationStep={generationStep}/>}
  {deleteTarget&&<DeleteQuarterModal quarter={deleteTarget} onCancel={()=>!deleting&&setDeleteTarget(null)} onConfirm={confirmDeleteQuarter} deleting={deleting}/>} 
 </div>
}

function QuarterWizard({form,setForm,participants,selected,setSelected,onClose,onGenerateActivate,busy,generationStep}:{form:{year:number;quarter:number;label:string};setForm:(f:{year:number;quarter:number;label:string})=>void;participants:Participant[];selected:number[];setSelected:(ids:number[])=>void;onClose:()=>void;onGenerateActivate:()=>void;busy:boolean;generationStep:GenerationStep}){return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4"><div className="card max-h-[90vh] w-full max-w-3xl overflow-auto p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><h2 className="text-2xl font-semibold">Generate Quarter</h2><p className="mt-1 text-sm text-slate-400">Choose the quarter and participants, then generate and activate it immediately. If it is wrong later, delete it and generate a replacement.</p></div><button onClick={onClose} disabled={busy} className="rounded border border-border px-3 py-1 disabled:opacity-50">Close</button></div><div className="mt-5 grid gap-3 md:grid-cols-3"><input type="number" value={form.year} disabled={busy} onChange={e=>setForm({...form,year:Number(e.target.value)})} className="rounded bg-bg p-3 disabled:opacity-60"/><select value={form.quarter} disabled={busy} onChange={e=>setForm({...form,quarter:Number(e.target.value)})} className="rounded bg-bg p-3 disabled:opacity-60">{[1,2,3,4].map(q=><option key={q} value={q}>Q{q}</option>)}</select><input value={form.label} disabled={busy} onChange={e=>setForm({...form,label:e.target.value})} placeholder="Optional label" className="rounded bg-bg p-3 disabled:opacity-60"/></div><h3 className="mt-6 font-semibold">Participants included in quarter generation</h3><div className="mt-3 flex gap-2"><button disabled={busy} onClick={()=>setSelected(participants.map(p=>p.id))} className="rounded border border-border px-3 py-1 text-sm disabled:opacity-50">Select all</button><button disabled={busy} onClick={()=>setSelected([])} className="rounded border border-border px-3 py-1 text-sm disabled:opacity-50">Clear</button><span className="px-3 py-1 text-sm text-slate-400">{selected.length} selected</span></div><div className="mt-3 grid max-h-64 gap-2 overflow-auto md:grid-cols-2">{participants.map(p=><label key={p.id} className="flex items-center gap-2 rounded bg-black/20 p-2 text-sm"><input type="checkbox" disabled={busy} checked={selected.includes(p.id)} onChange={e=>setSelected(e.target.checked?[...selected,p.id]:selected.filter(id=>id!==p.id))}/><span>{p.display_name}</span></label>)}</div>{generationStep&&<div className="mt-5 rounded border border-indigo-400/40 bg-indigo-500/10 p-4"><div className="flex justify-between text-sm"><span>{generationStep.label}</span><span>{generationStep.progress}%</span></div><div className="mt-2 h-2 rounded bg-black/30"><div className="h-2 rounded bg-indigo-400 transition-all" style={{width:`${generationStep.progress}%`}}/></div><p className="mt-2 text-xs text-slate-400">Detailed generation progress is also written to the backend container logs.</p></div>}<div className="mt-6 flex justify-end gap-3"><button onClick={onClose} disabled={busy} className="rounded border border-border px-4 py-2 disabled:opacity-50">Cancel</button><button onClick={onGenerateActivate} disabled={busy||selected.length<2} className="rounded bg-indigo-500 px-4 py-2 font-semibold disabled:opacity-50">{busy?'Generating...':'Generate and Activate Quarter'}</button></div></div></div>}

function DeleteQuarterModal({quarter,onCancel,onConfirm,deleting}:{quarter:Quarter;onCancel:()=>void;onConfirm:()=>void;deleting:boolean}){return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4"><div className="card w-full max-w-lg p-6 shadow-2xl"><h2 className="text-2xl font-semibold text-red-100">Delete {quarter.label}?</h2><p className="mt-3 text-sm text-slate-300">This removes the generated quarter and its plan unless sent-point history exists. This cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onCancel} disabled={deleting} className="rounded border border-border px-4 py-2 disabled:opacity-50">Cancel</button><button onClick={onConfirm} disabled={deleting} className="rounded bg-red-600 px-4 py-2 font-semibold disabled:opacity-50">{deleting?'Deleting...':'Delete Quarter'}</button></div></div></div>}

function Preview({detail,sent,recv}:{detail:QuarterDetail;sent:Map<number,number>;recv:Map<number,number>}){const byFrom=new Map<number,Plan[]>();const incoming=new Map<number,Plan[]>();detail.plan.forEach(p=>{if(p.from_participant_id)byFrom.set(p.from_participant_id,[...(byFrom.get(p.from_participant_id)||[]),p]);if(p.to_participant_id)incoming.set(p.to_participant_id,[...(incoming.get(p.to_participant_id)||[]),p])});return <div className="card p-4"><h2 className="font-semibold">Quarter allocation</h2><p className="mt-1 text-sm text-slate-400">This generated allocation is read-only.</p>{detail.plan.length===0?<p className="mt-3 text-slate-400">No quarter generated yet.</p>:<div className="mt-4 space-y-4">{detail.participants.map(p=><div key={p.id} className="rounded border border-border p-4"><div className="flex items-center justify-between"><h3 className="font-semibold">{p.display_name}</h3><span className={`text-sm ${(sent.get(p.id)||0)===50&&(recv.get(p.id)||0)===50?'text-green-300':'text-red-300'}`}>Sent {sent.get(p.id)||0}/50 · Received {recv.get(p.id)||0}/50</span></div><div className="mt-3 grid gap-3 lg:grid-cols-2"><div><h4 className="text-xs uppercase text-slate-400">Giving</h4>{(byFrom.get(p.id)||[]).map(row=><div key={row.id} className="mt-2 flex items-center justify-between rounded bg-black/20 p-3"><span>{row.to_name}</span><strong>{row.amount}</strong></div>)}</div><div><h4 className="text-xs uppercase text-slate-400">Receiving</h4>{(incoming.get(p.id)||[]).map(row=><div key={row.id} className="mt-2 rounded bg-black/20 p-3"><span>{row.from_name}</span><strong className="float-right">{row.amount}</strong></div>)}</div></div></div>)}</div>}</div>}
