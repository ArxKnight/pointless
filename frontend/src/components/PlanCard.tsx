import {Plan} from '../types/api';import {post} from '../lib/api';import {StatusBadge} from './Badges';
export function PlanCard({plans,onChange}:{plans:Plan[];onChange:()=>void}){const total=plans.reduce((s,p)=>s+p.amount,0), sent=plans.filter(p=>p.acknowledged).reduce((s,p)=>s+p.amount,0);return <div className="card p-6"><div className="mb-4 flex items-center justify-between"><div><h2 className="text-xl font-semibold">My Giving Plan</h2><p className="text-sm text-slate-400">{sent}/{total} points sent</p></div><div className="grid h-20 w-20 place-items-center rounded-full border-4 border-indigo-500/40 font-mono text-lg">{total?Math.round(sent/total*100):0}%</div></div><div className="space-y-3">{plans.map(p=><div key={p.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-border bg-black/10 p-4"><div><p className="font-medium">Send <span className="font-mono text-indigo-300">{p.amount}</span> points to {p.to_name}</p><StatusBadge sent={p.acknowledged}/></div><button disabled={p.acknowledged} onClick={async()=>{await post(`/plans/${p.id}/sent`);onChange()}} className="rounded-lg bg-indigo-500 px-4 py-2 text-sm font-semibold disabled:cursor-not-allowed disabled:bg-slate-700">Mark as Sent</button></div>)}</div></div>}
