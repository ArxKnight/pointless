import {useEffect,useState} from 'react';import {BrowserRouter,Navigate,Route,Routes} from 'react-router-dom';import {api} from './lib/api';import {User} from './types/api';import {Layout} from './components/Layout';import {Login} from './pages/Login';import {Install} from './pages/Install';import {Dashboard} from './pages/Dashboard';import {MyTree} from './pages/MyTree';import {History} from './pages/History';import {Members} from './pages/Members';import {Quarters} from './pages/Quarters';import {Settings} from './pages/Settings';
type InstallStatus={installed:boolean;database_configured:boolean};
export default function App(){const [installed,setInstalled]=useState<boolean|undefined>(undefined);const [user,setUser]=useState<User|null|undefined>(undefined);useEffect(()=>{api<InstallStatus>('/install/status').then(s=>{setInstalled(s.installed); if(s.installed) api<User>('/auth/me').then(setUser).catch(()=>setUser(null)); else setUser(null)}).catch(()=>{setInstalled(true);setUser(null)})},[]);if(installed===undefined||user===undefined)return <div className="grid h-screen place-items-center bg-bg">Loading...</div>;if(!installed)return <Install onInstalled={()=>{setInstalled(true);setUser(null)}}/>;return <BrowserRouter>{!user?<Routes><Route path="*" element={<Login setUser={setUser}/>}/></Routes>:<Routes><Route element={<Layout user={user} setUser={setUser}/>}><Route index element={<Dashboard/>}/><Route path="tree" element={<MyTree/>}/><Route path="history" element={<History/>}/><Route path="settings" element={<Settings/>}/>{user.is_admin&&<><Route path="members" element={<Members/>}/><Route path="quarters" element={<Quarters/>}/></>}<Route path="*" element={<Navigate to="/"/>}/></Route></Routes>}</BrowserRouter>}
